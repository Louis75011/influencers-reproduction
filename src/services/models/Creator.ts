import User from './implements/User'

export default class implements User {
    id: number
    name: string
    price: number
    quantity: number
    image: string
    isDisponible: Boolean
    detail: String
    order: number
  }